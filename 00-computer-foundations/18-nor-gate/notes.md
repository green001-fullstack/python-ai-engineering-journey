# What Does NAND Mean?

NAND simply means:

NOT AND

It's literally two operations performed one after another.

AND

↓

NOT

Think of it like a factory.

First, an AND gate computes its result.

Then, a NOT gate flips that result.

That's all a NAND gate is.