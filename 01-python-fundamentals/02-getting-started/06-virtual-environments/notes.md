An isolated Python environment with its own interpreter, installed packages, and configuration.

System Python
      │
      ├── Project A (venv)
      │       ├── NumPy 1.24
      │       └── Pandas
      │
      └── Project B (venv)
              ├── NumPy 2.0
              └── PyTorch